spring:
  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: test-only-secret-key-must-be-at-least-256-bits-long-for-hs256
  access-token-expiration-seconds: 7200

logging:
  level:
    org.hibernate.SQL: debug

